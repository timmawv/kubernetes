apiVersion: v1
kind: Service
metadata:
  name: tennis-service

spec:
  type: NodePort
  selector:
    app: tennis-label
  ports:
    - port: 8080 #порт, который идет наружу
      targetPort: 8080 # реальный порт имеджа, куда слать трафик