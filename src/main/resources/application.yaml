spring:
  application:
    name: Kubernetes

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info

app:
  environment: ${APP_ENVIRONMENT:local}
  greeting-message: ${GREETING_MESSAGE:Hello from default config}